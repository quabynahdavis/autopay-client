# Set this in the Vercel dashboard (Project → Settings → Environment Variables).
# This is the ONLY env var required. It is server-side only (never in the browser bundle).
# Point it to your deployed Render backend URL — no /api suffix.
INTERNAL_API_URL=https://autopay-server.onrender.com
