# Set this in your Vercel dashboard (Project → Settings → Environment Variables)
# Read server-side only by next.config.ts — NOT exposed to the browser bundle.
# Point it to your deployed Render backend URL (no /api suffix).
INTERNAL_API_URL=https://autopay-server.onrender.com
